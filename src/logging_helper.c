#include "logging_helper.h"

void app_log_battery_state(BatteryChargeState s) {
    //app_log(APP_LOG_LEVEL_DEBUG, __FILE__, __LINE__, "s.charge_percent: %d", s.charge_percent);
    //app_log(APP_LOG_LEVEL_DEBUG, __FILE__, __LINE__, "s.is_charging: %d", s.is_charging);
    //app_log(APP_LOG_LEVEL_DEBUG, __FILE__, __LINE__, "s.is_plugged: %d", s.is_plugged);
}
